{% extends 'base.html.twig' %}

{% block title %}Men{% endblock %}

{% block body %}
    <h1>Men</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ man.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ man.name }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ man.weight }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ man.height }}</td>
            </tr>
            <tr>
                <th>Index_weight</th>
                <td>{{ man.indexWeight }}</td>
            </tr>
            <tr>
                <th>Result</th>
                <td>{{ man.result }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('men_index') }}">back to list</a>

    <a href="{{ path('men_edit', {'id': man.id}) }}">edit</a>

    {{ include('men/_delete_form.html.twig') }}
{% endblock %}
